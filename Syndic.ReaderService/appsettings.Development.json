{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Debug"
    }
  },
  "ConnectionStrings": {
    "readerdb": "Run this project via the Aspire AppHost to ensure this value is populated with a valid connection string"
  }
}
